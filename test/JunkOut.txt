
process PROD  =
{
	source = EmptyInputService
	{
	  untracked int32 maxEvents = 20
	}

	module m1 = StreamThingProducer { int32 array_size = 10000 }
	module m2 = StreamThingProducer { int32 array_size = 10000 }

	module out = PoolOutputModule
	 {
 	   untracked string fileName = "junkout1.root"
	 }

        path p1 = { m1,m2,out }
}
