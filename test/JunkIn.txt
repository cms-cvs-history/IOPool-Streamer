
process PROD  = {

        source = PoolInputService
        {
                untracked string fileName = 'junkout1.root'
                untracked int32 maxEvents = -1
        }

	module out = PoolOutputModule
	 {
 	   untracked string fileName = "junkout2.root"
	 }

        path p1 =  { out }
}
